const SingleAchievement = ({ achievements }: { achievements: any }) => {
  const { url, icon, dark_icon, sub_title, title, year } = achievements;

  return (
    //
    <div></div>
  );
};

export default SingleAchievement;
